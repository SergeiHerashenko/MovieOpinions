# Зроблено сьогодні (21 серпня 2026)

### Вихід із системи та refresh-токени

- [x] Реалізовано та перевірено use case виходу користувача із системи.
- [x] Додано транзакційне читання refresh-токена з блокуванням рядка через `SELECT ... FOR UPDATE`.
- [x] Відкликання refresh-токена виконується через агрегат `User`.
- [x] Зміна стану refresh-токена зберігається через `AggregateChanges` у межах тієї самої транзакції.
- [x] Узгоджено поведінку паралельних запитів `Refresh` і `Logout`: перший запит блокує потрібний рядок, другий продовжує роботу після завершення транзакції та перевіряє вже актуальний стан.
- [x] Виправлено отримання ідентифікатора користувача з claims авторизованого запиту.

### Завантаження агрегату та транзакційний persistence

- [x] Винесено спільне завантаження повного агрегату `User` в окремий `AdoUserAggregateLoader`.
- [x] Повторно використано aggregate loader у query- та command-репозиторіях.
- [x] Loader підтримує роботу як зі звичайним connection, так і з connection та активною `NpgsqlTransaction`.
- [x] Реалізовано `GetUserByIdForUpdateAsync`, який завантажує користувача та вкладені сутності в межах однієї транзакції.
- [x] Розділено звичайне читання агрегату й транзакційне читання з блокуванням.

### AggregateChanges та domain events

- [x] Остаточно розділено призначення `AggregateChanges` і domain events.
- [x] Реалізовано `IAggregateChangesDispatcher` та MediatR-обробники для збереження конкретних змін агрегату.
- [x] `AggregateChanges` виконуються всередині `UnitOfWork`, до commit транзакції.
- [x] Domain events обробляються окремо після успішного збереження основних змін.
- [x] Дописано та виправлено події, пов’язані з діями користувача й сесіями обмежень.
- [x] Події обмежень перенесено на семантичний рівень сесії, щоб одна подія могла описувати групу пов’язаних обмежень.
- [x] Розділено події оновлення сесії обмежень за змістом операції додавання та видалення обмежень.

### Модель очікуваних дій користувача

- [x] Узагальнено модель `UserPendingChange` до `UserPendingAction`.
- [x] Створено ієрархію `UserAction` для різних типів очікуваних дій.
- [x] Додано `DeleteAccountAction` для ініціації видалення облікового запису.
- [x] Додано value object `DeletionReason` для причини видалення.
- [x] Реалізовано створення, відновлення, підтвердження, завершення, невдале завершення та прострочення pending action.
- [x] Додано перевірку типу дії, confirmation token і строку дії перед виконанням видалення.
- [x] Прострочена активна дія переводиться у відповідний стан і більше не блокує створення нової дії.

### Контракти з Contact Service

- [x] Загальний `ContactsRequest` розділено на вузькі DTO: `CreateContactRequest`, `UpdateContactRequest` і `DeleteContactRequest`.
- [x] Окремо описано request- і response-моделі для отримання активних контактних каналів.
- [x] Реалізовано отримання списку активних контактів користувача через `ContactsSender`.
- [x] Реалізовано отримання одного вибраного активного контакту за `UserId` і `ContactId`.
- [x] Endpoint Contact Service винесено в options і підтримано підстановку `userId` у шаблон маршруту.
- [x] Відповідь Contact Service перетворюється на власну модель результату use case, без витоку інтеграційного DTO у зовнішній API.
- [x] Для адреси доставки сповіщення використовується окремий транспортний контракт із `ContactValue` та `CommunicationChannel`, без помилкового перетворення контакту на `Login`.

### Видалення облікового запису — початок флоу

- [x] Реалізовано use case `StartDeletingUser`.
- [x] Додано валідацію IP-адреси, UserId, пароля та rate limiting.
- [x] Додано fake password verification для сценарію, коли користувача не знайдено.
- [x] Перед створенням pending action користувач повторно завантажується з `FOR UPDATE`.
- [x] Усередині транзакції повторно перевіряється актуальний стан видалення користувача.
- [x] Гарантовано, що одночасно для користувача існує не більше однієї активної pending action.
- [x] Створення `UserPendingAction` та його persistence виконуються атомарно.
- [x] Після створення дії запитуються доступні контактні канали.
- [x] Use case повертає непрозорий `ConfirmationToken` та власні DTO доступних каналів із маскованими значеннями.

### Видалення облікового запису — надсилання підтвердження

- [x] Реалізовано use case `SendDeletionConfirmation`.
- [x] Додано валідацію `ContactId`, UserId, IP-адреси та rate limiting.
- [x] Перевіряється наявність активної дії саме типу `DeleteAccountAction`.
- [x] Перевіряються confirmation token і строк життя pending action.
- [x] Вибраний контакт повторно перевіряється через Contact Service, а значення контакту не приймається на довіру від frontend.
- [x] Запит на сповіщення формується з ідентифікатором pending action, перевіреним контактом, каналом і типом повідомлення.
- [x] Для email і телефону повертаються відповідний наступний крок та повідомлення для інтерфейсу.
- [x] `ConfirmationToken`, отриманий на першому кроці, зберігається frontend і повторно використовується в наступних запитах.
- [x] Тимчасова недоступність інтеграційного сервісу не переводить pending action у стан `Failed`, тому користувач може повторити надсилання.

### Видалення облікового запису — підтвердження та завершення

- [x] Реалізовано use case `ConfirmationDeleting`.
- [x] Use case приймає `ConfirmationToken` і значення підтвердження з SMS-коду або URL.
- [x] Додано перевірку UserId, IP-адреси та rate limiting для підтвердження видалення.
- [x] Перед зверненням до зовнішнього сервісу перевіряється користувач, активна pending action, її тип, token і строк життя.
- [x] Перевірка коду або посилання виконується через Verification Service поза локальною транзакцією PostgreSQL.
- [x] Після успішної верифікації користувач повторно завантажується з `FOR UPDATE` у межах `UnitOfWork`.
- [x] Під блокуванням повторно перевіряється актуальна pending action, тому паралельний запит не може вдруге завершити ту саму дію.
- [x] Видалення виконується доменним методом `ConfirmDeleting` через `CompleteAction<DeleteAccountAction>`.
- [x] Причина видалення береться з `DeleteAccountAction`, а не повторно приймається від frontend.
- [x] Створюється запис про видалення користувача та змінюється стан агрегату `User` на видалений.
- [x] Pending action підтверджується, перестає бути активною та більше не завантажується як поточна дія користувача.
- [x] Усі пов’язані `AggregateChanges` зберігаються атомарно в одній транзакції.
- [x] Повний flow видалення облікового запису завершено та перевірено: ініціація → вибір каналу → надсилання підтвердження → перевірка коду/URL → видалення користувача.