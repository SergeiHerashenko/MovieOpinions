# Зроблено сьогодні (29.06.2026)

## Шар Infrastructure

- [x] Реалізовано повноцінний ADO.NET-репозиторій для роботи з тимчасовими реєстраціями (`UserPendingRegistration`).

- [x] Реалізовано CRUD-операції для сутності:
  - створення запису;
  - оновлення даних;
  - видалення;
  - пошук за логіном.

- [x] Додано автоматичне відновлення доменної сутності з даних бази (`Restore`) із підтримкою різних типів логіну (Email / Phone).

- [x] Реалізовано централізоване мапування `NpgsqlDataReader` → доменна модель із використанням Ordinals для оптимізації читання даних.

- [x] Додано відновлення телефонного логіну з розділенням коду країни та номера телефону.

- [x] Винесено створення SQL-параметрів в окремий допоміжний метод для повторного використання.

- [x] Реалізовано детальне логування всіх операцій репозиторію (створення, оновлення, видалення).

- [x] Додано перевірки цілісності даних під час відновлення сутностей із генерацією інфраструктурних винятків при пошкоджених або неконсистентних даних.

- [x] Реалізовано використання `RETURNING *` для отримання актуального стану сутності після виконання операцій зміни даних.

- [x] Побудовано репозиторій на базі спільної інфраструктури (`RepositoryBase`) із централізованою роботою з підключеннями до PostgreSQL.